# Crawler (Scrapy)

This service owns:

- Domain discovery workflows
- Platform detection via custom fingerprints + WhatWeb outputs
- Page classification hooks
- Lead scoring signals emitted to backend

## Bootstrap plan

Initialize a Scrapy project rooted in `crawler/`.
