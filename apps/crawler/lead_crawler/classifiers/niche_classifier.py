"""Niche classification placeholder."""

from __future__ import annotations


class NicheClassifier:
    """Classifies stores into business niches."""

    def classify(self, domain: str, signals: dict[str, str]) -> str:
        """Return a placeholder niche label for a domain."""
        _ = (domain, signals)
        return "unclassified"
