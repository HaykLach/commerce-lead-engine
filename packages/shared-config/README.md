# Shared Config

Configuration package consumed by backend + crawler:

- Scoring weights and thresholds
- Fingerprint signatures and normalization rules
- Page classification taxonomy and rules

Use JSON/YAML files to keep scoring configurable without code redeploys.
